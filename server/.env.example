DATABASE_URL="postgresql://hrms:hrms@localhost:5432/hrms"
JWT_SECRET="change-me"
PORT=4000
# Only needed if Chrome/Chromium isn't at one of the default paths salarySlip.ts checks
# PUPPETEER_EXECUTABLE_PATH="C:/Program Files/Google/Chrome/Application/chrome.exe"
# Comma-separated allowlist of frontend origins. Unset = reflect any origin
# (fine for local dev with no deployed frontend yet).
# CORS_ORIGIN="https://app.example.com,https://admin.example.com"
